require "spec_helper"

describe GalpinVeiwTool do
  it "has a version number" do
    expect(GalpinVeiwTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
